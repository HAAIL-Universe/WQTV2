// role.js - shared shell for role pages
import { getRole, clearSession } from './auth.js';

document.addEventListener('DOMContentLoaded', () => {
  // Optionally, show role or add logout later
  // For now, just a shell
});
